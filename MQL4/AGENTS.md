# MQL4 Agent Guide

Keep MQL4 include guards project-prefixed and path-derived. Preserve the public
facade contract and keep examples aligned with the C++ naming and version
policy where the APIs correspond.

MetaEditor is not part of the standard local toolchain. Treat MQL4 scripts as
manual checks and report an aggregate result when running them.

